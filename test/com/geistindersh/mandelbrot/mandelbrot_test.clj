(ns com.geistindersh.mandelbrot.mandelbrot-test
  (:require [clojure.test :refer [are deftest]]
            [com.geistindersh.mandelbrot.mandelbrot :refer []]))

;; TODO Add Tests
(deftest dummy
  (are [a b] (= a b)
             1 1))